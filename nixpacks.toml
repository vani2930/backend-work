[phases.setup]
nixPkgs = ["nodejs_22", "python313"]

[phases.install]
cmds = [
  "npm install",
  "python3 -m ensurepip --upgrade",
  "python3 -m pip install -r requirements.txt"
]

[start]
cmd = "npm start"